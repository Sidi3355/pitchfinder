# Used by `vite build --mode test` for the local and CI end-to-end runs. The
# fake Supabase in tests/e2e/fake-supabase.mjs listens here.
VITE_SUPABASE_URL=http://localhost:4177
VITE_SUPABASE_ANON_KEY=test-anon-key
# The OSRM table stub in the same fake server; see tests/e2e/fake-supabase.mjs.
VITE_OSRM_URL=http://localhost:4177/osrm
